import { styled } from "@/styles";

export const ProductTableContainer = styled("div", {
  display: "flex",
  flexDirection: "column",
  height: "100%",
  width: "100%",

  header: {
    marginBottom: "2.5rem",
  },
});
